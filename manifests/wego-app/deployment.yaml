apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: wego-app
  name: wego-app-development
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: wego-app
    spec:
      serviceAccountName: wego-app-service-account
      containers:
        - name: wego-app
          image: ghcr.io/weaveworks/wego-app:{{.Version}}
          args: ["ui","run"]
          ports:
            - containerPort: 9001
          imagePullPolicy: Always
  selector:
    matchLabels:
      app: wego-app
