[flux]
version="0.29.5"
special_tarpath="https://github.com/fluxcd/flux2/releases/download/v${version}/flux_${version}_${goos}_${goarch}.tar.gz;flux"

[envtest]
version="1.19.2"
special_tarpath="https://storage.googleapis.com/kubebuilder-tools/kubebuilder-tools-${version}-${goos}-${goarch}.tar.gz;kubebuilder/bin"

[tilt]
version="0.23.8"
special_tarpath="https://github.com/tilt-dev/tilt/releases/download/v${version}/tilt.${version}.${goos}.${arch}.tar.gz;tilt"

[helm]
version="3.8.1"
special_tarpath="https://get.helm.sh/helm-v${version}-${goos}-${goarch}.tar.gz;${goos}-${goarch}/helm"
