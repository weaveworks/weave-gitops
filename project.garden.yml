kind: Project
name: wego-app
environments:
  - name: local
    variables:
      default-hostname: wego-app.local.app.garden
providers:
  - name: local-kubernetes
    environments: [local]
    buildMode: cluster-buildkit
    setupIngressController: nginx